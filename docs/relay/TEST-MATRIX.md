# テストマトリクス

`npm test` で全て走ります。実行結果は [RESULTS.md](RESULTS.md)。

---

## 1. 自動テスト

### 1.1 上流から引き継いだもの（74 件・全て維持）

| ファイル | 件数 | 何を守っているか |
|---|---|---|
| `fountain.test.ts` | 16 | LT 符号、robust soliton 分布、`dlog` のゴールデン値、フレーム索引 |
| `protocol.test.ts` | 15 | 20 バイトヘッダー、`DCF2` コンテナ、SHA-256、gzip 上限、stream identity |
| `no-signal.test.ts` | 9 | 「反応が無い」ヒントの出し方 |
| `worker-pool.test.ts` | 7 | 復号ワーカーのスロット同一性 |
| `frame-capacity.test.ts` | 7 | 1 枚あたりの容量とブロック数の上限 |
| `progress.test.ts` | 6 | 噴水符号の余剰見積もりと残り時間 |
| `snippet.test.ts` | 5 | テキスト片のコンテナ |
| `qr-raster.test.ts` | 5 | モジュール行列 → ピクセル |
| `format.test.ts` / `display.test.ts` | 4 | 表示の整形と寸法合わせ |

**上流のゴールデンベクタは 1 つも変えていません。** フォルダー転送は新しい
ペイロード種別であって、新しいワイヤーフォーマットではないためです。

### 1.2 追加したもの（81 件）

| ファイル | 件数 | 何を守っているか |
|---|---|---|
| `relative-path.test.ts` | 9 | 悪意あるパス 37 種、長さ・深さの上限、NFC、集合としての衝突 |
| `bundle.test.ts` | 19 | `DCB1` の往復、上流コンテナとの同居、宣言と実測の食い違い、数値の細工、上限、ハッシュ不一致 |
| `slow-frames.test.ts` | 16 | `DCS1` のゴールデン値、順不同・重複・欠落・CRC 破損・別文書混入・大文字小文字・終端文字 |
| `writer.test.ts` | 13 | 上書き回避、部分書き込みの残留なし、リンク、権限、Mark of the Web |
| `relay-e2e.test.ts` | 19 | 実 HTTPS サーバー相手の署名・再送・改竄・上限・ペアリング・失効・時計ずれ |
| `optical-loop.test.ts` | 4 | QR 描画 → 実復号 → 噴水 → 検証 → **バイト一致**（カメラ不要） |
| `optical-e2e.test.ts` | 1 | 擬似カメラ経由の全経路（この環境では skip、理由つき） |

### 1.3 主要な脅威ごとの対応表

| 脅威 | どのテストで | 期待する結果 |
|---|---|---|
| `../` によるパストラバーサル | `relative-path` / `bundle` / `writer` | 拒否。理由つき |
| 絶対パス・ドライブ相対・UNC | `relative-path` / `bundle` | 拒否 |
| 円記号を含むパス | `relative-path` | 拒否（区切りは `/` のみ） |
| NTFS 副ストリーム `a.txt:x` | `relative-path` | 拒否 |
| Windows 予約名 `CON.txt` | `relative-path` / `writer` | 拒否 |
| RLO・ゼロ幅による表示偽装 | `relative-path` | 拒否 |
| NFD と NFC の同名衝突 | `relative-path` | NFD を拒否 |
| 大文字小文字違いの同名 | `bundle` | 拒否 |
| ファイルとフォルダーの名前衝突 | `relative-path` / `bundle` / `writer` | 拒否 |
| 深すぎる階層 | `relative-path` | 拒否 |
| 負・小数・巨大な size | `bundle` | 拒否（オフセットが逆走しない） |
| manifest 長が buffer 外 | `bundle` | 読まずに拒否 |
| 末尾に余分なバイト | `bundle` | 拒否 |
| 個々のハッシュ不一致 | `bundle` / `relay-e2e` | 拒否。**1 件も保存しない** |
| 件数・総サイズの上限超え | `bundle` | 宣言値・実測値の両方で拒否 |
| 欠落フレーム | `optical-loop`（3 枚に 1 枚を落とす） | 完走してバイト一致 |
| 破損フレーム | `optical-loop`（画面を横切る帯） | 捨てて完走 |
| 順不同フレーム | `optical-loop`（逆順に投入） | バイト一致 |
| 重複フレーム | `slow-frames` / 上流 `fountain` | 1 回だけ数える |
| 低速モードの番号化け | `slow-frames` | 行全体の CRC で撥ねる |
| 別文書のコードの混入 | `slow-frames` | 拒否 |
| 同じ文書の別の刷り | `slow-frames` | 拒否（大きさ不一致） |
| 未登録端末 | `relay-e2e` | 401、ホストの時刻つき |
| 登録コードの総当たり | `relay-e2e` | 5 回でコード破棄 |
| 登録コードの再使用 | `relay-e2e` | 1 回だけ |
| 署名後の本体改竄 | `relay-e2e` | 400。**何も書かれない** |
| 要求の再送 | `relay-e2e` | 2 回目は 401 |
| 他端末の id での署名 | `relay-e2e` | 401 |
| 別パスへの署名の流用 | `relay-e2e` | 401 |
| 端末の失効 | `relay-e2e` | 即座に 401 |
| 巨大な Content-Length | `relay-e2e` | 読む前に 413 |
| 他サイトからの呼び出し | `relay-e2e` | 403。ペアリングは常に拒否 |
| 端末の時計ずれ | `relay-e2e` | 理由を返し、次の要求は通る |
| 同名の保存 | `relay-e2e` / `writer` | `名前 (2)`。元は無傷 |
| 保存の途中失敗 | `writer` | 痕跡を残さない |

---

## 2. 実機・実 GUI での確認

| 対象 | どうやったか | 結果 |
|---|---|---|
| Windows デスクトップアプリ | `pc\pub-transfer.ps1` で**実際に起動**し、DevTools プロトコルで **実ウィンドウの実コントロール**を操作 | [RESULTS.md](RESULTS.md) |
| 送る（選ぶ→表示→終了） | 実フォルダーを実際に読み、実バンドルを作り、**実 QR ストリームを実画面に表示** | 完走 |
| 受け取る（リーダー低速モード） | 実 `DCS1` フレーム 12 枚を入力欄へ。2 枚を意図的に飛ばして欠番表示を確認、その後補って完走 | 完走・照合成功 |
| カメラなし | このデスクトップに**本当にカメラが無い**状態をそのまま撮影 | 実状態 |
| カメラ拒否 | ページの実エラー経路（`getUserMedia` の拒否と同じ関数）で再現 | 実経路 |
| スマホ PWA | **実 relay ホスト**（HTTPS・実証明書・実署名）に対し、3 つの実機相当幅で操作 | 完走 |
| ペアリング | 実コードを実発行 → 実入力 → 実トークン発行 | 完走 |
| 母艦→スマホ→表示 | 実 outbox から実取得し、実 QR ストリームを表示 | 完走 |

---

## 3. この環境で確認できなかったもの

[UNVERIFIED.md](UNVERIFIED.md) に、理由と人が確かめる手順を書いてあります。
要約:

- 実カメラでの受信（このデスクトップにカメラが無く、擬似カメラもこの環境の
  Chromium / WebView2 では利用できなかった）
- 物理の二次元コードリーダー（HID / COM）
- 物理スマートフォン
- 実 Tailscale の複数端末間
