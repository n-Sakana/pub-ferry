# Ferry バグチェック・修正報告書

作成日: **2026-09-08**  
対象: 提供された `ferry.zip`  
対象 ZIP の SHA-256: `04d1a3d525fd1cde78dfa9ef8f1794402204d41f198e0175367fa3daa9d1a1c9`

## 1. 結論と検証の到達点

一般的な誤動作につながる非同期処理・状態管理と、QR 認識／転送処理を修正しました。
特に、サーバーの全要求直列処理、表示セッションの上書き、受信通信エラーによる即時停止、
重複した QR の再送、全画面画像を毎回処理する負荷、リモコンの一覧更新による QR 表示停止を見直しています。

**修正済みソースを納品しますが、すべての実機動作を保証できる段階ではありません。**
今回の環境には .NET SDK／C# コンパイラー、Windows、実カメラがなく、
**変更した C# のコンパイル・実行、Windows での起動、実カメラでの転送は未検証**です。
以下の「合格」は、実行できた範囲に限定した結果です。

| 検証対象 | 結果 | 検証方法・範囲 |
|---|---:|---|
| JavaScript 単体試験 | **20 / 20 合格** | ヘッダー検証、受信キュー、再試行、重複排除、停止競合、座標、圧縮データ展開 |
| ブラウザ機能チェック | **16 / 16 合格** | Chromium、同梱 WASM、実際の HTML/CSS/JS、仮想カメラ。API はモック |
| 合成画像の QR 認識 | **旧設定 13 / 15、新設定 15 / 15** | 同じ画面上の大きさで、旧 2953-byte QR と新 1000-byte QR を比較 |
| JS 構文・静的資産整合性 | 合格 | 全 JS、HTML の参照 ID、埋め込み資産、読み込み順序を確認 |
| C# 回帰試験 | **未実行** | 11 件を追加。テストプロジェクト自体のビルドも未確認 |
| Windows 起動・実サーバーの HTTP 結合試験 | **未実行** | PowerShell 5.1 / WPF / WebView2 を含む |
| 実カメラの転送速度・成功率 | **未測定** | スマホ、画面、照明、距離を含む実機条件では未確認 |
| 実 Office 文書の Markdown 化・VBA 抽出 | **未実行** | 画面遷移は確認。実文書の変換結果は確認していない |

試験ログ・詳細値は [`tests/results/`](tests/results/)、再実行手順は [`tests/README.md`](tests/README.md) にあります。

## 2. 一般的な不具合・誤動作の確認と修正

### 2.1 ほかの処理が QR 配信まで止めてしまう

**原因:** `LocalServer.RunAsync` が `GetContextAsync` → `HandleSafelyAsync` の完了を待ってから、
次の要求を受け付けていました。ファイル選択ダイアログ、Markdown/VBA 処理、端末探索、QR 生成などが
すべて同じ待ち行列を共有し、別の要求を待たせる構造です。

**修正:** HTTP 処理を上限 16 件の並行実行に変更しました。
ファイル選択・変換・送信開始など、重複実行したくない操作には別の排他制御を置き、
通常の QR 配信・受信・状態確認とは切り離しています。終了時にダイアログを無制限に待たないようにもしました。

**確認範囲:** コードレビューのみ。並行 HTTP の実行試験、CPU 負荷、Windows のダイアログは未確認です。
対象: `src/Server/LocalServer.cs`。

### 2.2 新しい表示開始が別端末の QR を無効化する

**原因:** 送信サービスが `_active` 一つだけを保持していました。
一方、リモコン命令は接続中の複数端末へ送られるため、後から開始した表示が前の token を無効にします。

**修正:** token ごとのセッション保持に変更し、最大 4 表示を独立して管理します。
停止は指定 token だけに適用します。10 分間要求のないセッションを整理し、上限超過には説明付きの要求エラーを返します。

**確認範囲:** ソース修正と未実行の C# 回帰試験。複数実端末での確認は未実施です。
対象: `src/Optical/OpticalService.cs`。

### 2.3 リモコンのファイル一覧更新が QR を閉じる

**原因:** リモコンの定期的なフォルダー再取得も `applyFolder` を通り、
フォルダー情報が変わると進行中の光学表示を停止していました。
また、初期フォルダー取得より先に届いたリモコン命令を、後着した初期処理が上書きする余地がありました。

**修正:** 単なるメタデータ更新では表示を維持する経路を追加しました。
リモコン命令の受信開始も初期画面設定後に移しています。ユーザーが明示的に入力を変更する経路は従来どおり区別します。

**確認:** ブラウザ試験で、フォルダー情報の変更を定期取得しても QR 表示が継続することを確認しました。
Tailnet 自体はモックです。対象: `web/app.js`。

### 2.4 画面サイズ変更で QR が消える／停止後に遅い応答が残る

Canvas の幅・高さの再設定後に、最後に描いた QR を直ちに再描画するよう変更しました。
上下の操作部・説明部が使う高さも計算に入れ、QR が画面外へはみ出さないようにしています。
描画は端末ピクセル上で整数倍にそろえ、画像の補間を無効にしています。

開始要求・カメラ・Worker に世代を持たせ、停止前の遅い応答を次の処理に混ぜないようにしました。
先読み fetch の中断、遅れて成功した送信開始 token の解放、ページ離脱時の停止通知も追加しています。

**確認:** 390×844、844×390、1280×800 の画面で QR が不透明かつ画面内に残ること、
表示を閉じると停止 API が呼ばれること、受信キューの停止後の応答が処理されないことを確認しました。
すべての非同期順序を網羅した検証ではありません。対象: `web/app.js`、`web/optical-core.js`。

### 2.5 カメラ初期化失敗を位置合わせの失敗に見せてしまう

元 Worker は WASM の読み込み失敗を握りつぶした後も準備完了相当の応答を返していました。
その場合、利用者からは何度位置を合わせても QR が見つからない状態に見えます。

準備完了は WASM の初期化成功後だけにし、初期化エラー・致命的エラー・準備タイムアウトを通知します。
カメラ権限拒否時の無条件な再要求もやめ、撮影条件を満たせないエラーの場合だけ条件を緩めて再要求します。[R2]
画面起動中のカメラ処理にも中断後のチェックを追加し、使わない映像トラックを止めます。

**確認:** WASM 初期化失敗の通知、カメラ拒否時の要求が 1 回であること、
停止時に `video.srcObject` が解放されることをブラウザで確認しました。
対象: `web/qr-worker.js`、`web/app.js`。

### 2.6 そのほかの一般修正

| 項目 | 修正内容 | 検証範囲 |
|---|---|---|
| 誤り訂正と格納量の不整合 | M/Q/H の容量を超える UI 選択を無効化し、サーバー側でも容量内に調整 | UI と JS の容量表を確認。C# は未実行 |
| 受信済みデータの破棄・混在 | 最初に認識した転送を固定し、別の転送で進捗を置換しない | JS/Worker は確認。C# は未実行 |
| 受信再開時の競合 | 開始ごとに新しい receiverId を生成。停止済み ID への遅延 POST をサーバーでも拒否 | ID 更新と JS 停止処理を確認。C# は未実行 |
| 完了後のメモリー保持 | 完了応答を保存し、重い復号器を解放。未使用受信状態を整理 | コードレビューのみ |
| 並行出力先の衝突 | 出力フォルダー名の選択と作成をプロセス内で排他化 | C# 試験追加、未実行。別プロセスとの完全な排他ではない |
| 画面スリープ防止 | Wake Lock の保持・解放、送受信での利用、可視状態に戻ったときの再要求 | コードレビューのみ。OS の制約による拒否は許容 |
| 小さいファイルの後で設定が小さいままになる | 小容量の自動調整値を次の大容量転送の設定に持ち越さない | ブラウザで確認 |

## 3. QR の「当たり判定」の改善

### 3.1 固定された中央の判定枠が原因ではない

提供コードでは、中央の小さな領域だけで認識可否を判定する処理は確認できませんでした。
カメラ画像は全画面を処理しており、プレビューの CSS も `object-fit: contain` でした。
したがって、存在しない「中央の当たり判定」を広げる変更はしていません。

問題に関係する要因として、高密度 QR、毎回の全画面デコード負荷、別 QR の混入、
初期化失敗の隠蔽、通信エラー時の停止がコードから確認できました。
ただし、実際のカメラで主原因がどれだったかは、この環境では確定していません。
ピント、反射、動き、露出、画面とカメラのタイミングも実機で切り分ける必要があります。

### 3.2 認識処理の変更

初回は全画面を探索します。一度 QR が見つかったら、認識した四隅の座標から、
周囲に余裕を持たせた領域を追跡します。中央固定ではなく、QR が動いた位置を使います。
追跡領域には最小 32 ピクセル、または QR の大きい辺の 35% の余白を設けます。
切り出した画像をさらに縮小する処理は入れていません。

6 回に 1 回は全画面を再探索します。3 回の認識失敗、認識から 1.2 秒以上の経過、
撮影解像度の変更でも全画面に戻します。QR を見失っても、それまでの受信ブロックは捨てません。
空の映像や未検出時は取り込みを約 15 回/秒に抑え、認識後はカメラのフレームに合わせます。[R3]

同時に最大 4 個の QR 候補を読み、単なる URL ではなく転送ヘッダーを検証したデータを選びます。
転送開始後はその転送識別子も照合し、ほかの転送へ追跡が移るのを抑えます。
軽い認識で見つからなければ回転・反転・縮小探索を含む強い読み取りへ切り替え、
一部の失敗フレームでは別の二値化方法も試します。[R1]

**白い余白を含む QR 全体が見える必要は残ります。**
欠けた QR や、模様を分離できないほどのぼけを必ず復元できる修正ではありません。

### 3.3 既定の QR 密度を下げた

| 設定 | 提供版の既定 | 修正版の既定 |
|---|---:|---:|
| 1 枚の最大格納量 | 2953 bytes | **1000 bytes** |
| 表示目標 | 60 fps | **30 fps** |
| 誤り訂正 | L | L |
| 比較用バイナリー QR のサイズ | version 40、余白込み 185×185 modules | version 22、余白込み 113×113 modules |

同じ表示サイズなら、一つ一つの模様を大きくでき、1 枚を表示している時間も長くなります。
小さなファイルは、内容に応じてさらに小さい QR へ自動調整します。

**これは安定性を優先する既定値です。良好な撮影環境での理論上の最大速度は下がります。**
20-byte ヘッダーを除いた単純計算では、旧既定は 2933×60＝175,980 bytes/秒、
新既定は 980×30＝29,400 bytes/秒です。圧縮率、取りこぼし、噴水符号の追加フレームを含まない値です。
新しい既定値だけで常に高速になる、という意味ではありません。
2953 bytes と 60 fps の選択肢は残し、その設定でも先読み・配信圧縮・追跡処理が使えるようにしています。

## 4. 転送速度・中断耐性の改善

### 4.1 QR を RGBA 画像ではなくビット列でまとめて配信

従来は、QR の 1 module を RGBA 4 bytes として 1 枚ずつ HTTP で取得していました。
修正版は白黒 1 bit/module にし、通常 8 枚をまとめて取得します。
先読みは最大 24 枚、通信は最大 2 要求並行です。

追加した `/api/optical/frames` の応答形式は次のとおりです。

```text
FQR1                  4 bytes
side                  uint16 little endian
count                 uint16 little endian (1–16)
firstSequence         uint32 little endian
bitmaps               ceil(side × side / 8) bytes × count
                      行優先、MSB first、黒=1、白=0、4-module の余白込み
```

version 40、余白込み 185×185、8 枚で比較すると、画像本文は次のサイズです。

| 形式 | 本文サイズ |
|---|---:|
| 従来の RGBA 8 枚 | 1,095,200 bytes |
| 修正版の packed 8 枚（12-byte ヘッダー込み） | 34,244 bytes |
| 比率 | **約 1/31.98、約 96.9% 減** |

これは形式・配列長から算出し、JavaScript の展開試験で確認した値です。
**画面とカメラの間の光学転送が約 32 倍になるという意味ではありません。**
C# での QR 生成時間、HTTP/TLS ヘッダー、ネットワークの実測時間は含みません。
ブラウザへ送る QR 画像データと要求回数を減らす改善です。

表示が遅れた後に複数枚を急速に追い付かせる動作も抑え、次の QR が未準備なら直前の QR を残します。
画面には目標 fps だけでなく、実際に描画した平均 fps と実際の bytes/QR を表示します。
これはカメラの認識 fps を示す表示ではありません。

### 4.2 受信を順序付きキューにして再試行

従来は同時 POST が 4 件あると、それ以上の読み取り結果を捨てていました。
さらに、一時的な POST エラーでもカメラと受信状態を停止していました。

修正版は転送 ID＋フレーム番号で重複排除し、最大 128 枚の待機キューを持ちます。
同時 POST は 1 件、1 回の送信は最大 8 枚です。成功済みの重複照合履歴も上限 2048 件に制限しています。
ネットワークエラー、HTTP 408 / 429 / 5xx では同じバッチを再試行し、待機時間は 250 ms から最大 5 秒へ増やします。
それ以外の恒久的な 4xx は利用者へエラーを示して停止します。

**通信復旧は、同じサーバープロセスと受信画面が生きている場合の再試行です。**
ページ再読み込み、サーバー再起動、受信状態の有効期間超過を越えてディスクから再開する機能ではありません。
長い断線中にキュー上限を超えたフレームは保存できませんが、上限内の未送信データは保持します。

### 4.3 同じ画像による読み取り処理の限定測定

1280×960 の同一の 1000-byte QR 合成画像を使い、旧 Worker の全画面処理と、
修正 Worker の追跡領域処理を比較しました。ウォームアップ 3 回後、各 15 回を測定しました。

| 項目 | 旧経路 | 新経路 |
|---|---:|---:|
| 処理画像 | 1280×960 | 693×726 の追跡領域 |
| 画素数 | 1,228,800 | 503,118（約 59.1% 減） |
| Worker 応答までの中央値 | **10.0 ms** | **5.0 ms** |
| 復号した bytes | 元データと一致 | 元データと一致 |

測定区間は RGBA 取得後の Worker 送受信と WASM 読み取りです。
映像取得、Canvas からの画素取り出し、HTTP POST、C# の噴水符号復元、ファイル保存は含めません。
この一条件では約半分の時間でしたが、CPU・QR の大きさ・明るさに依存します。
また、新経路では認識オプションも変えているため、切り出しだけの効果を分離した測定ではありません。

## 5. 合成画像・ブラウザ試験の内容

画像は 1280×960 の背景に生成し、中央、四隅、中心からのずれ、15 度／30 度回転、台形変形、
2 段階のぼけ、300／250／200 ピクセルへの縮小、通常のリンク QR との同時表示を試しました。
旧設定は 15 条件中 13 条件、新設定は 15 条件すべてで元のフレーム bytes と一致しました。
旧設定で一致しなかったのは強いぼけと 200 ピクセルへの縮小です。

中央・四隅では旧設定も成功しているため、「旧版には中央以外を拒否する判定があった」という結果ではありません。
密度と処理の両方を変更した比較であり、**15 / 15 を実カメラの 100% 成功率に読み替えることはできません。**

ブラウザ試験では実際の HTML/CSS/JS を用い、仮想カメラが異なる 12 枚の QR を順次映す構成で、
同梱 WASM の認識から受信キューまでを動かしました。
最初の受信要求を一時エラーにし、再試行後に異なる 6 フレームが届いた時点でモックが完了応答を返します。
受信画像から得た bytes が元データと同一であることも照合しています。

**この試験の完了表示は、実際のファイル保存が成功した証拠ではありません。**
スクリーンショットの経過時間・速度・保存先もモック値です。
ブラウザの URL ナビゲーション制限があったため、制限を変更せず、資産をメモリーから渡す試験モードを使いました。
実サーバーの CSP、Service Worker 更新、HTTP ルーティング、C# との結合は実行検証していません。

## 6. 互換性・変更範囲

光学 QR 内の `D1 0C` ヘッダー、噴水符号、DCF2/DCB1 コンテナー形式は変更していません。
`FountainEncoder` 以降の生成・梱包処理、`OpticalFrame` 以降の読み取り・復元・検算処理が
提供ソースと同一であることを静的に照合しています。新しい `FQR1` はローカル HTTP での画像配信形式だけです。
旧 `/api/optical/frame`、`/api/optical/receive/frame` も残しています。
ただし、他実装との相互運用や旧クライアントとの組み合わせを実行試験したわけではありません。

同梱 WASM、ZXing.Net、WebView2 DLL などの依存ファイルは更新せず、提供 ZIP とバイト単位で同一です。
ライセンス・第三者表示も保持しています。
変更した C# は元の Windows PowerShell 起動経路を考慮して C# 5 相当の書き方にとどめましたが、
コンパイルできることや .NET Framework 上で動くことは今回確認できていません。

主な変更ファイル:

| ファイル | 内容 |
|---|---|
| `src/Server/LocalServer.cs` | 並行処理、packed 配信、受信バッチ API |
| `src/Optical/OpticalService.cs` | 複数 token、容量調整、小容量 QR、ビット配信 |
| `src/Optical/OpticalReceiveService.cs` | 転送固定、遅延 POST 対策、完了時解放、状態整理 |
| `src/Common/OutputLayout.cs` | 同一プロセス内の並行出力名衝突対策 |
| `src/Server/StaticAssets.cs`、`Ferry.csproj` | 新しい JS の配信・埋め込み、試験ソースの除外 |
| `web/app.js` | 先読み、再試行、追跡、開始停止競合、リモコン、リサイズ、fps 表示 |
| `web/optical-core.js`（追加） | ヘッダー検証、packed 展開、座標計算、受信キュー |
| `web/qr-worker.js` | 明示的な初期化結果、転送 QR 選別、段階的な認識 |
| `web/zxing-reader.js`（追加） | 既存 Worker から認識ライブラリー部分を分離 |
| `web/index.html`、`web/service-worker.js` | 既定値、説明、読み込み・キャッシュ資産 |
| `tests/`（追加）、`README.md` | 再現試験、結果、起動案内・未検証範囲 |

## 7. 納品物と起動

ZIP は `ferry/` をルートとしたソース一式です。
`src/`、`web/`、`lib/`、`common/`、`ferry.vbs`、`ferry.cmd`、`ferry.ps1`、
インストール／アンインストールスクリプト、ライセンス、試験、報告書を含みます。
**修正を反映しない生成済みの `bin/`・`obj/`・`artifacts/` と Git 履歴 `.git/` は含めていません。**
そのため提供 ZIP より大幅に小さくなっていますが、実行に必要な同梱ライブラリーを省いたわけではありません。

旧版を終了し、別フォルダーへ全展開して `ferry.vbs` を起動する構成です。
従来どおり Windows PowerShell 5.1 がソースをコンパイルし、WPF + WebView2 を使用します。
WebView2 Runtime は別途必要です。問題が出た場合は `ferry.cmd` と `%LOCALAPPDATA%\Ferry\logs` が確認箇所です。
ここに記した起動経路は提供ソースの構成説明であり、今回の Windows 起動成功を表すものではありません。

PC とスマホのページは、両方とも修正版サーバーから再読み込みしてください。
新しい画面は新しいバッチ API を使うため、古いサーバーと新しい画面を混在させないでください。

## 8. 実機で残る確認事項

最優先は Windows 上でのコンパイルと起動、および小さいファイルの送受信から保存までの確認です。
次に、画面端・距離変更・反射・一時的な通信断・カメラの停止再開を試し、保存ファイルの SHA-256 を比較します。
PC とスマホ、複数リモコン、Office 実文書の変換も別途確認が必要です。

速度は、まず新しい既定値で実際の完了時間を測り、安定して読める場合だけ 1 枚の格納量や fps を上げて比較してください。
大きな QR や高 fps を選ぶほど必ず速くなるわけではなく、認識成功率との兼ね合いになります。
比較には同じファイル、同じ画面サイズ・距離・照明・撮影設定を使う必要があります。

残る制約として、カメラが失った模様の復元、OS のバックグラウンド制限、サーバー再起動を越える再開、
長時間のメモリー／CPU 負荷、同時利用上限付近での応答、ディスク不足時の実動作は未確認です。
今回の静的レビューと限定試験だけで、文書変換を含む全機能に不具合が残っていないとは判定していません。

## 9. 参照資料・根拠

修正の主な根拠は提供されたソースと、同梱の再現テスト・結果ログです。
API の意味については以下の一次資料を参照しました。
ライブラリーの最新版へ更新したわけではなく、実際に使ったのは提供 ZIP にある zxing-wasm 2.2.4 の実装です。

- **[R1]** zxing-wasm `ReaderOptions`: <https://sec-ant.github.io/zxing-wasm/docs/interfaces/full.ReaderOptions.html>
- **[R2]** MDN `getUserMedia` とエラー: <https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getUserMedia>
- **[R3]** MDN `requestVideoFrameCallback`: <https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement/requestVideoFrameCallback>

ログ: [`node-tests.txt`](tests/results/node-tests.txt)、[`browser-tests.txt`](tests/results/browser-tests.txt)、
[`browser-results.json`](tests/results/browser-results.json)、[`static-checks.txt`](tests/results/static-checks.txt)。
